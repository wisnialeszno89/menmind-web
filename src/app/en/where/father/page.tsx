import StateTemplate from "@/components/where/StateTemplate";

export default function EnFatherPage() {
  return (
    <StateTemplate
      title="Father in crisis"
      description="Responsibility, pressure, and very little margin for error."
      stateKey="father"
    />
  );
}
