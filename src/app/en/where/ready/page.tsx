import StateTemplate from "@/components/where/StateTemplate";

export default function EnReadyPage() {
  return (
    <StateTemplate
      title="Ready for change"
      description="Something closed. There is room to move forward."
      stateKey="ready"
    />
  );
}
