import StateTemplate from "@/components/where/StateTemplate";

export default function EnBreakupPage() {
  return (
    <StateTemplate
      title="After a breakup"
      description="The relationship ended, but the emotions are still loud."
      stateKey="breakup"
    />
  );
}
