import StateTemplate from "@/components/where/StateTemplate";

export default function EnEmptyPage() {
  return (
    <StateTemplate
      title="Stable but empty"
      description="Nothing is broken. Nothing feels right either."
      stateKey="empty"
    />
  );
}
