import StateTemplate from "@/components/where/StateTemplate";

export default function EnBrokenPage() {
  return (
    <StateTemplate
      title="Broken / angry"
      description="This is the moment where fixing things too fast usually makes them worse."
      stateKey="broken"
    />
  );
}
