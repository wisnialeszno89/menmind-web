import StateTemplate from "@/components/where/StateTemplate";

export default function BreakupPage() {
  return (
    <StateTemplate
      title="Po rozstaniu"
      description="Relacja się skończyła, ale emocje jeszcze nie.
Myśli krążą w kółko, a decyzje trudno podjąć na chłodno."
      stateKey="breakup"
    />
  );
}
